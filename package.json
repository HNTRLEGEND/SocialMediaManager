{
  "name": "wieslogic-platform",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "prettier": "3.2.5",
    "turbo": "^1.12.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  }
}
