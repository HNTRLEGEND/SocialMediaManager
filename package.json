{
  "name": "wieslogic-platform",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "engines": {
    "node": ">=18.18.0",
    "pnpm": ">=8.15.4"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "3.2.5",
    "turbo": "2.0.0"
  }
}
