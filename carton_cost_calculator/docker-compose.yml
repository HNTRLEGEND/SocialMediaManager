version: '3.8'

services:
  carton-calculator:
    build: .
    container_name: karton-kostenrechner
    ports:
      - "8501:8501"
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_ENABLE_CORS=false
